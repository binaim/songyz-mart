~ Please follow the following steps to run the application

~ docker compose down -v -> stops all the running servers like the DB servers or Kafka.

~ docker compose up -> starts new Docker containers of the servers mentioned in the docker-compose file